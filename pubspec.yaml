name: fb88
description: "FB88 Project"

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:



  flutter:
    sdk: flutter
  shared_preferences: ^2.3.4
  cupertino_icons: ^1.0.8
  dio: ^4.0.1          # HTTP client
  retrofit: ^2.0.0     # Retrofit adapter
  json_annotation: ^4.5.0 # JSON
  jwt_decoder: ^2.0.1
  build_runner: ^2.2.0
  json_serializable: ^6.0.0
  intl: ^0.18.0
  url_launcher: ^6.1.10


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_animate: ^4.0.0
  flutter_lints: ^4.0.0
  retrofit_generator: ^2.0.0


flutter:
  uses-material-design: true
  assets:
  - res/images/